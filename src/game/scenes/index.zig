pub const Menu = @import("menu.zig");
pub const Game = @import("game.zig");

pub const util = struct {
    pub const FPS = @import("util/fps.zig");
    pub const Checkerboard = @import("util/checkerboard.zig");
};
